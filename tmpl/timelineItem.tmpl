Template.set('timelineItem',
(function () {/*
<div class="item" data-name="{{product.id}}">
	<div class="crop">
		<div class="fravr-btn">
		<div class="map">
			<button type="button" class="btn btn-default btn-lg btn-top btn-left btn-success btn-frav">
				<span class="glyphicon glyphicon-star"></span> Frav
			</button>
			<button type="button" class="btn btn-default btn-lg btn-top btn-right btn-share">
				<span class="glyphicon glyphicon-share-alt"></span> Share
			</button>
			<button type="button" class="btn btn-default btn-lg btn-bottom btn-left btn-clip">
				<span class="glyphicon glyphicon-paperclip"></span> Clip
			</button>
			<button type="button" class="btn btn-default btn-lg btn-bottom btn-right btn-cart">
				<span class="glyphicon glyphicon-shopping-cart"></span> Cart
			</button>
		</div>
		</div>
		<img src="{{product.img_urls.0.url}}">
	</div>
	<a href="product.htm?product={{product.id}}"><span class="title">{{product.name}}</span></a>
	{{#if actions}}
		{{#ifequals actions.length 0}}
		<span class="subtitle">by {{product.merchant}}</span>
		{{else}}
			{{#ifequals actions.length 1}}
				{{#ifequals actions.0.action 'purchase'}}
					<span class="subtitle">by {{product.merchant}}</span>
				{{/ifequals}}
			{{else}}
			<span class="subtitle">by {{product.merchant}}</span>
			{{/ifequals}}
		{{/ifequals}}
	{{else}}
	<span class="subtitle">by <a href="#">{{product.merchant}}</a></span>
	{{/if}}
</div>
*/}).toString().split('\n').slice(1, -1).join('\n'));
